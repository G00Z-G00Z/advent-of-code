rust:
  init_commands:
    - cargo init --vcs none --lib -q

  files:
    - name: .env.example
      content: |
         DEMO_MODE=0
    - name: .env
      content: |
         DEMO_MODE=1
    - input.txt
    - demo-input.txt

