rust:
  init_commands:
    - cargo init --vcs none --lib -q

  files:
    - name: .env.example
      content: |
         DEMO_MODE=0
    - name: .env
      content: |
         DEMO_MODE=1
    - input.txt
    - demo-input.txt
    - name: src/lib.rs
      content: |
        #[cfg(test)]
        mod tests {
            use utility_2022::{get_input, is_demo_mode};

            use super::*;

            pub mod part1 {
            }

            pub mod part2 {
            }
        }


